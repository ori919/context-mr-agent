# Sample Context Brief Output

This is a **real output** generated by the Context Brief Agent on
[MR !1](https://gitlab.com/ila-group/context-mr-agent/-/merge_requests/1).

---

## 🔍 Context Brief

### What changed
- **`config/auth.ts`** — New auth configuration file centralising token expiry (7200s), rate-limit window (15 min / 5 requests), session settings (`httpOnly`, `secure` gated on `NODE_ENV`), and the reset email base URL sourced from `RESET_URL` env var.
- **`migrations/20260325_add_recovery_tokens_table.sql`** — Irreversible migration adding `recovery_tokens` (UUID PK, `token_hash`, `expires_at`, `used`, `ip_address`, `user_agent`) and `reset_rate_limits` (IP-keyed counter) tables, with two indexes on `user_id` and `expires_at WHERE used = FALSE`.
- **`src/api/auth/reset-password.ts`** — Two new Express routes: `POST /api/v1/auth/reset-password/request` (rate-limited, email-enumeration-safe response) and `POST /api/v1/auth/reset-password/confirm` (validates token, updates password, invalidates all sessions). `findUserByEmail`, `updateUserPassword`, and `invalidateAllSessions` are currently stubs returning hardcoded values.
- **`src/components/PasswordResetForm.tsx`** — Four-step React component (`request → sent → confirm → done`) that drives the full reset UX; token is entered manually by the user in the `confirm` step.
- **`src/services/email-service.ts`** — `EmailService.sendPasswordReset()` builds a reset link as `${RESET_URL}?token=<raw_token>` and calls a no-op `send()` stub — no real email provider is wired up yet.
- **`src/services/rate-limiter.ts`** — In-process `RateLimiter` using a `Map` keyed by `req.ip`; state is not persisted and resets on server restart.
- **`src/services/token-service.ts`** — `TokenService` stores recovery tokens in an in-memory `Map` (not the database); generates tokens with `crypto.randomBytes(32)`, stores the **raw value** (not the hash), and marks tokens `used` on invalidation.

### Why this matters
This MR introduces the entire self-service password reset surface — a security-critical flow that touches auth, database schema, API contracts, session management, and the frontend simultaneously. Several components are explicitly incomplete (stub functions, no email provider, in-memory-only token storage), meaning the feature is not production-ready and carries meaningful security risk if merged as-is.

### Affected areas
- **Session management** — `invalidateAllSessions` is a stub; existing active sessions for a user will not actually be terminated after a password change.
- **Database** — `recovery_tokens.token_hash` column exists in the schema but `TokenService` stores and validates the **raw token value** in memory, never writing to or reading from the DB. The migration and the service are currently disconnected.
- **Email delivery** — `EmailService.send()` is a no-op placeholder; no SMTP/SES/SendGrid integration exists, so reset emails will never be sent.
- **Rate limiting** — `RateLimiter` state lives in process memory; it is ineffective in multi-instance deployments and resets on every restart. The `reset_rate_limits` DB table is created but never used by the service.
- **Password validation** — `updateUserPassword` is a stub; no password strength policy, hashing (bcrypt/argon2), or DB write is implemented.
- **Frontend routing** — `PasswordResetForm` is not wired to any route or page; it is unclear how users navigate to it.

### Hidden assumptions
- **Token storage mismatch**: The migration creates a `token_hash` column (implying hashed storage), but `TokenService` stores the raw token in memory and never persists to the DB. If the intent is to hash tokens before storage, the service must be updated before the DB schema is meaningful.
- **Raw token in reset link**: `EmailService` appends the raw token directly to the URL (`?token=<value>`). If tokens are later hashed in the DB, the lookup strategy must change.
- **`RESET_URL` env var must be set in all environments**: The fallback `https://app.example.com/reset` is a placeholder domain. If `RESET_URL` is missing in staging/production, reset links will point to a non-existent host.
- **`session.secure` is `false` in non-production**: Cookies will be sent over HTTP in development/staging, which may not match the team's security posture for those environments.
- **Migration is irreversible**: The MR description and the migration comment both confirm there is no `DOWN` migration. Rolling back the deployment requires manual DB intervention.
- **No tests**: The MR description explicitly states no tests exist for the new endpoints. The `src/api/auth/` and `src/services/` directories are new, so there is no baseline coverage to compare against, but this is a security-sensitive flow.

### Risk level
**High**
Auth flow, session invalidation, database migration (irreversible), and token security are all in scope — and several critical pieces (token persistence, password hashing, session teardown, email delivery) are unimplemented stubs.

### Reviewer checklist
- [ ] Confirm `TokenService` is updated to hash tokens (e.g. SHA-256) before storage and to write/read from the `recovery_tokens` DB table — not an in-memory `Map`.
- [ ] Verify `updateUserPassword` is implemented with a proper password hashing algorithm (bcrypt or argon2) and actually writes to the DB.
- [ ] Verify `invalidateAllSessions` is implemented to actually terminate active sessions for the user (check session store — DB, Redis, etc.).
- [ ] Confirm `EmailService.send()` is wired to a real email provider before this is deployed to any environment where users will trigger resets.
- [ ] Assess whether the in-memory `RateLimiter` is acceptable or whether it must be backed by the `reset_rate_limits` DB table (or Redis) for multi-instance safety.
- [ ] Check that `RESET_URL` is defined in all deployment environment configs (staging, production) and that the placeholder domain is not reachable.
- [ ] Confirm the irreversible migration has been reviewed by someone with DB rollback authority and that a manual rollback runbook exists.
- [ ] Validate that `session.secure: false` in non-production is intentional and acceptable per the team's security policy.
- [ ] Confirm `PasswordResetForm` is mounted on a route and that the token is passed via URL param (not manual entry) when the user clicks the email link.
- [ ] Add tests covering: valid token flow, expired token rejection, used token rejection, rate limit enforcement, and email-enumeration-safe response for unknown emails.

### Docs follow-up
- Document the password reset API endpoints (`POST /api/v1/auth/reset-password/request` and `/confirm`) in the API reference, including request/response shapes, error codes, and rate-limit behaviour.
- Add a runbook for manually rolling back the `recovery_tokens` migration, since it is explicitly irreversible.
- Document the required environment variables (`RESET_URL`, `NODE_ENV`) and their expected values per environment in the deployment/configuration guide.

---

*Generated by Context Brief Agent*
